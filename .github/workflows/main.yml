name: Python package

on:
  push:
    branches:
      - main
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macOS-latest]
        python-version: ['3.7', '3.8', '3.9', '3.10']

    steps:
    - uses: actions/checkout@v2

    # Clear pip cache (For Ubuntu/Linux)
    - name: Clear pip cache (Ubuntu/Linux)
      if: runner.os == 'Linux'
      run: rm -rf ~/.cache/pip

    # Clear pip cache (For Windows)
    - name: Clear pip cache (Windows)
      if: runner.os == 'Windows'
      run: Remove-Item -Recurse -Force "$Env:LOCALAPPDATA\pip\Cache"

    # Clear pip cache (For macOS)
    - name: Clear pip cache (macOS)
      if: runner.os == 'macOS'
      run: rm -rf ~/Library/Caches/pip

    # Set up Python
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}

    # Upgrade pip
    - name: Upgrade pip
      run: python -m pip install --upgrade pip

    # Install Nox for test orchestration
    - name: Install Nox
      run: pip install nox

    # Run tests
    - name: Run tests
      run: nox

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    # Set up Python 3.x
    - name: Set up Python 3.x
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'

    # Install twine for package upload
    - name: Install twine
      run: pip install twine

    # Build and publish to PyPI
    - name: Build and publish
      env:
        TWINE_USERNAME: ${{ secrets.PYPI_USERNAME }}
        TWINE_PASSWORD: ${{ secrets.PYPI_PASSWORD }}
      run: |
        python setup.py sdist bdist_wheel
        twine upload dist/*
